# gp-008 — Explicit Claude Code subagent models, effort and permissions

STATUS: DONE
OWNER: Claude Code

Give Claude Code runs concrete tiers: simple Sonnet 5 / medium, complex Sonnet 5 /
xhigh, expert Opus 5 / xhigh. Claude Code accepts a per-spawn model but reads
effort only from agent frontmatter (still true in 2.1.270 docs); the user chose
role-default pinning (`claude.role_tiers`) over per-tier agent copies, and a
fallback to generator defaults for 1.1 projects with an empty `claude` block.

Follow-up approved by the user (2026-09-13): tool-enforced read-only 2D reviewer and
verifier (no Bash), project-scoped `.claude/settings.json` allow rules merged by
bootstrap, and `claude.max_turns` (runner: 8).

Scope: model policy template and schema, gp_work.py routing/dispatch, lib/claude.sh
adapters, runbooks, role templates, marketplace texts, docs and regressions.
Released as 1.2.0. Live Claude Code dispatch with the pinned settings is not
verified here.

# gp-006 — Dual-harness Game Pipeline marketplace

STATUS: done
OWNER: Codex
DATE: 2026-09-04

## Scope

Package `game-pipeline` as a repo-local marketplace for Claude Code and Codex,
following the proven `mobile-pipeline` layout. Keep one `gp-dev` plugin, one
canonical runtime source, and no paired baseline or model benchmark workflow.

## Delivered

- `.claude-plugin/marketplace.json` and `.agents/plugins/marketplace.json`.
- Generated `claude-plugins/gp-dev` with `/gp`, dimension-specific lazy runtime
  fallbacks, skill metadata and a bundled generator wrapper.
- Generated `codex-plugins/gp-dev` with `$gp-dev`, runtime references and the same
  bundled generator wrapper.
- `lib/build-marketplace.sh` with archive-before-replace behavior and `--check`.
- `docs/MARKETPLACE.md`, README/USAGE installation instructions and v0.4.0.

## Validation

- Codex `validate_plugin.py`: pass.
- `claude plugin validate`: pass with non-fatal warnings for frontmatter on
  lazy-loaded internal runbooks and an ignored Claude `interface` field.
- Marketplace JSON, build script syntax and catalog check: pass.
- Packaged generator dry-run and both preset paths remain covered by the
  existing regression suite.

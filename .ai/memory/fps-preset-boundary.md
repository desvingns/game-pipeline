---
name: fps-preset-boundary
description: v0.3 uses selected 3D contracts and actual Godot/Blender evidence
---

One generator, two initial presets. 2D sim stays engine-free; 3D separates pure
domain rules from Godot world physics/navigation. Only pure rules use exact
replay hashes. Human and bot inputs share production paths. --build completes one
approved brief; the user explicitly rejected a paired baseline experiment.

Model frontmatter is omitted for both tools so every role inherits the session
model. Blender GLBs carry exact artifact/source/profile/reference hashes. Keep
art/builds production records with the assets. Review runtime materials, not just
Workbench previews. Export smoke needs an explicit flag separate from ordinary
QA host arguments. Copy only source files from overlays; __pycache__ must not
break subsequent bootstrap runs.

Current evidence and limits: docs/VALIDATION-0.3.md. Personal gp-dev was reinstalled.

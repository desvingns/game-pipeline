# STATE — {{PROJECT_NAME}}

_Last updated: {{TODAY}} (scaffolded by game-pipeline v{{GP_VERSION}})._

## Where this is

Nothing implemented yet. The pipeline is installed; the game is not started.

## Gate readiness

| Gate | Status |
|---|---|
| build + tests | needs a Godot project under `game/` |
| determinism | needs `res://tools/gates/sim_harness.tscn` |
| balance | needs the harness and content tables |
| assets | ready — needs Python with Pillow |
| style lock | not locked |
| screenshots | needs `res://tools/gates/shot.tscn` |

## In flight

_nothing_

## Blocked

_nothing_

## Next step

Run `/{{PREFIX}} --style` first. Everything visual is downstream of the style
bible, and starting art or scenes before STYLE LOCK produces work that has to be
redone.

Then `/{{PREFIX}} --design <the core loop>`.

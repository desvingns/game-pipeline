<!-- gp-runtime-contracts: startup, 3d -->

# --gates

Read-only health check. Run every required gate and retain each JSON result:

1. runner-godot.sh — real import and nonempty unit assertions.
2. sim-godot.sh --replay — pure-rule replay in independent processes.
3. style-lock.sh --verify — reference stability.
4. mesh-validate.sh --all — every GLB, including missing-provenance failures.
5. fps-godot.sh --all — movement/combat/enemies/route/restart/UI/visual/perf;
   the selected network module adds actual peer scenarios.
6. runner-godot.sh --export — when a Windows delivery is requested.

Use .codex/scripts/gp- for every command. Do not fix source during
this mode. Early-project missing harnesses/assets are readiness gaps, not n/a or
passes. Technical pass does not establish subjective art/audio/control quality.
Return one GATES block with each JSON, BLOCKING, REVIEW_REQUIRED, EXPORT_PATH.
